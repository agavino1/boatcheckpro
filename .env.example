# ──────────────────────────────────────────
# BoatCheckPro Backend – Environment Variables
# Copy this file to .env and fill in the values
#
# PRODUCTION: estas variables se pasan via GitHub Secrets
#             (ver DEPLOY_GUIDE.md > Paso 4)
# LOCAL:      rellena aquí y renombra a .env
# ──────────────────────────────────────────

# Server
NODE_ENV=development
PORT=3000             # Cloud Run usa 8080 (se sobreescribe automáticamente)

# ── Base de datos ──────────────────────────
# Supabase (producción): Settings > Database > Connection string
# Local: usa PostgreSQL local o la URL de Supabase directamente
DB_HOST=db.XXXXXXXXXXXX.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=tu_password_de_supabase

# ── Autenticación JWT ──────────────────────
# Genera una clave aleatoria: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=cambia_esto_por_una_clave_aleatoria_de_64_chars
JWT_EXPIRES_IN=7d
REFRESH_TOKEN_SECRET=otra_clave_aleatoria_diferente_64_chars

# ── URLs ───────────────────────────────────
# FRONTEND_URL: la URL de Vercel (para CORS)
FRONTEND_URL=https://boatcheckpro.vercel.app
# API_URL: la URL de Cloud Run (para links en emails)
API_URL=https://boatcheckpro-api-XXXX-ew.a.run.app

# ── Stripe ─────────────────────────────────
# Obtén las keys en: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# ── Email (Nodemailer) ─────────────────────
# Gmail: usa App Password (no tu contraseña normal)
# Gmail > Gestionar cuenta > Seguridad > Contraseñas de aplicación
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=tu@gmail.com
EMAIL_PASSWORD=tu_app_password_de_gmail
EMAIL_FROM=BoatCheckPro <noreply@boatcheckpro.com>

# ── Admin inicial ──────────────────────────
ADMIN_EMAIL=admin@boatcheck.pro
ADMIN_PASSWORD=CambiaMeEnProduccion123!
